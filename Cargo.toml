[package]
name = "batteryctl"
version = "1.1.0"
edition = "2021"
author = "DucktectiveCZ"
license = "MIT"

[dependencies]
clap = { version = "4.3", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

